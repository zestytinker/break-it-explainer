{
  "name": "break-it-explainer",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive explainers that let you build a tech concept, then break it",
  "scripts": {
    "build": "node pipeline/render.js",
    "test": "node pipeline/render.js && node --test",
    "generate": "node pipeline/run.js",
    "coverage": "node pipeline/render.js && node --test --experimental-test-coverage --test-coverage-exclude='test/**' --test-coverage-exclude='node_modules/**'"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5"
  }
}
