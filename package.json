{
  "name": "break-it-explainer",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive explainers that let you build a tech concept, then break it",
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node --test",
    "coverage": "node build.js && node --test --experimental-test-coverage --test-coverage-exclude='**/*.test.js' --test-coverage-exclude='dom-helper.js'"
  },
  "devDependencies": { "jsdom": "^30.0.1" }
}
